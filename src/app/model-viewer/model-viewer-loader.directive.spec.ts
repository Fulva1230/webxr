import { ModelViewerLoaderDirective } from './model-viewer-loader.directive';

describe('ModelViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelViewerLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
