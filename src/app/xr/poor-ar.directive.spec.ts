import { PoorArDirective } from './poor-ar.directive';

describe('PoorArDirective', () => {
  it('should create an instance', () => {
    const directive = new PoorArDirective();
    expect(directive).toBeTruthy();
  });
});
