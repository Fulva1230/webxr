import { BuscardDirective } from './buscard.directive';

describe('BuscardDirective', () => {
  it('should create an instance', () => {
    const directive = new BuscardDirective();
    expect(directive).toBeTruthy();
  });
});
