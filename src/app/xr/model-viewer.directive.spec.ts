import { ModelViewerDirective } from './model-viewer.directive';

describe('ModelViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelViewerDirective();
    expect(directive).toBeTruthy();
  });
});
